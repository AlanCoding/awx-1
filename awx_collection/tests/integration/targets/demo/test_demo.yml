- hosts: localhost
  gather_facts: no
  tasks:
  - tower_organization:
      name: demo

  - tower_inventory:
      name: demo
      organization: demo

  - tower_host:
      name: demo
      variables: "ansible_connection: local"
      inventory: demo
